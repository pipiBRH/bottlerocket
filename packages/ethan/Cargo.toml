[package]
name = "ethan"
version = "0.1.0"
edition = "2018"
license = "Apache-2.0"
description = "Ethan service"
publish = false
build = "../build.rs"

[lib]
path = "../packages.rs"

[package.metadata.build-package]

# RPM BuildRequires
[build-dependencies]

# RPM Requires
[dependencies]
